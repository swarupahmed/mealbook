import 'package:flutter/material.dart';

class JoinManager extends StatelessWidget  {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(
        title:Text(' Join a Manager Panel'),
      ),
      body:Container() ,
    );
  }
}

